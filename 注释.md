# 注释

注释，面向用户。用于解释代码的目的、设计意图、约束条件或复杂逻辑。

Python 解释器通常会忽略注释，不把它作为程序逻辑执行。

注释分为*单行注释、多行注释和文档字符串*。

# 单行注释

使用 `#` 表示单行注释。

```python
# 计算商品折扣后的价格
original_price = 100
discounted_price = original_price * 0.8
print(discounted_price)
```

输出：

```text
80.0
```

单行注释可以写在代码后面：

```python
age = 18  # 用户年龄
```

*但行尾注释应少用*。若注释较长或逻辑重要，应单独占一行，代码更易读：

```python
# 用户年龄
age = 18
```

# 多行注释

Python 没有专门的“多行注释符号”。

*可以使用多个 `#` 来代替：*

```python
# 读取用户输入的成绩。
# 将成绩转换为整数。
# 判断成绩是否及格。
score = int(input("请输入成绩："))
is_passed = score >= 60
```

或者可以使用*三引号包围文字*：

```python
"""
这是一段看起来像多行注释的内容。
"""

'''
多行字符串
'''
```

此外，三引号可以保存换行和缩进，还可以减少转义符的使用：

```python
message = """第一行
第二行
第三行"""
print(message)
```

输出：

```text
第一行
第二行
第三行
```

```python
text = """他说："Python 很简单。" """
print(text)
```

输出：

```text
他说："Python 很简单。" 
```

# 文档字符串

三引号最重要的用途之一，是为模块、类、函数和方法编写文档字符串：

```python
def add(a, b):
    """返回两个数字的和。"""
    return a + b
```

可以通过 `__doc__` 访问：

```python
print(add.__doc__)
```

输出：

```text
返回两个数字的和。
```