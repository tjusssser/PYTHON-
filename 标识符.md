# 标识符规则

标识符（Identifier）用于变量、函数、类、模块、对象等名称的符号。

规则：
1. 只能由字母、数字、下划线_组成
2. 不能以数字开头
3. 不能包含空格或者特殊字符如-,@,#等
4. 不能使用python关键字
5. 严格区分大小写

```SQL
# 正确
name = 'tom'
student_1 = "Alice"
_age = 18

# 错误
1student = "Tom"     # 不能以数字开头
student-name = "Tom" # 不能使用连字符
student name = "Tom" # 不能包含空格
class = "Python"     # class 是关键字
```

# 判断是否为关键字
常见关键字包括
```PY
if 
else
for
while
def
class
try
except
raise
None
True
False
import
from
```
等等
*使用 `keyword.iskeyword()` 判断一个标识符是否为关键字*
- True表示是关键字
- False不是关键字
  ```PY
import keyword

print(keyword.iskeyword('if'))
print(keyword.iskeyword('a'))
```
* 使用`keyword.kwlist` 查看所有关键字*
```PY
import keyword
keyword.kwlist
```
# 规范性
好的标识符应该让代码接近自然语言，减少注释依赖。

