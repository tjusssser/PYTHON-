# join
把一个可迭代对象中的多个字符串，按指定分隔符连接成一个新字符串。
语法：
```py
separator.join(iterable)
```
其中：
- separator：分隔符，本身是一个字符串
- iterable：可迭代对象，里面的每一项都必须是字符串
例如：
```py
words = ["Python", "is", "beautiful"]
result = " ".join(words)
print(result)
```
输出：
```py
Python is beautiful
```

分隔符常用','、'/n'、'""'等连接。
```py
lines = ["第一行", "第二行", "第三行"]
print("\n".join(lines))
```
推荐使用join而不是+连接字符串。
1. join 性能更好
2. 代码清晰，可读性强
3. 更符合 Python 风格
案例 1：生成路径片段






















