# 概述
字符串属于不可变序列，用于表示文本数据。
Python 字符串支持：
- 索引
- 切片
- 遍历
- 成员判断
- 拼接
- 格式化
- 查找和替换
- 大小写转换
- 分割和连接

# 字符串的创建
Python 支持单引号和双引号：
```py
text1 = 'python' ;
text2 = "python" ;

print(text1 == text2)  # True
```
> ==注意==`==`和`is`的区别
```py
a='ajasdb1@' ;
b='ajasdb1@' ;
a==b;   #True
a is b ;  #False
```

>
当字符串内部包含引号时，可以交替使用：
```py
sentence = "I'm a Python programmer"
quote = 'He said "Hello".'
```
也可以使用反斜杠转义：
```py
text = "He said \"Hello\"."
path = "C:\\Users\\Alice"
```

更推荐使用原始字符串表示 Windows 路径或正则表达式：
```py
path = r"C:\Users\Alice"
print(path)
```
# 多行字符串
使用三引号创建多行字符串：
```py
text = """
第一行
第二行
第三行
"""

print(text)
```
三引号也常用于函数、类和模块的文档字符串：
```py
def add(a, b):
    """返回两个数的和。"""
    return a + b
```

# 字符串索引
字符串的索引从 0 开始
访问越界会产生 IndexError
> 参考 [索引类型](序列类型.md)
# 字符串切片
判断字符串是否为回文：
```py
def is_palindrome(text: str) -> bool:
    return text == text[::-1]


print(is_palindrome("level"))  # True
print(is_palindrome("Python")) # False
```
# 字符串不可变
字符串创建后，不能直接修改其中的某个字符：
```py
text = "Python"

# text[0] = "J"  # TypeError
```
正确方式是创建新字符串：
```py
text = "Python"
new_text = "J" + text[1:]

print(new_text)  # Jython
```

==字符串方法通常也会返回新字符串==：
```py
text = "python"

result = text.upper()

print(text)    # python
print(result)  # PYTHON
```
这体现了字符串的不可变性。
# 字符串拼接
使用 + 拼接少量字符串：
```py
first_name = "Guido"
last_name = "Rossum"

full_name = first_name + " " + last_name
print(full_name)
```

重复字符串可以使用 *：
```py
print("-" * 20)
# --------------------
```
不推荐在循环中反复使用 + 拼接大量字符串：
```py
# 不推荐
result = ""

for number in range(5):
    result += str(number)
```
大量字符串推荐使用 join()：
```py
parts = ["Python", "is", "powerful"]

result = " ".join(parts)

print(result)  # Python is powerful
```
`join`用于把一个可迭代对象中的多个字符串，按指定分隔符连接成一个新字符串。
见[字符串函数.md](字符串函数.md)

# 字符串格式化
见
## 常用字符串方法
见[字符串函数.md](字符串函数.md)
# 字符串与数字转换
字符串转整数：
```py
number = int("123")
print(number + 1)  # 124
```
字符串转浮点数：
```py
price = float("19.99")
```
数字转字符串：
```py
text = str(123)
print(text)  # "123"
```
不能直接连接字符串和数字,正确写法：
```py
print("年龄：" + str(age))
print(f"年龄：{age}")
```
# 字符串遍历
字符串属于可迭代序列。
```py
text = "Python"

for character in text:
    print(character)
```
































