# enumerate
在遍历可迭代对象时，同时拿到元素的下标和值
基本语法:
```py
enumerate(iterable, start=0)
```
其中：
- iterable：任何可迭代对象，如列表、字符串、元组、字典等
- start：起始下标，默认从 0 开始

很多初学者会这样写：
```py
items = ["a", "b", "c"]

for i in range(len(items)):
    print(i, items[i])
```
这能工作，但不够 Pythonic。更推荐：

```py
items = ["a", "b", "c"]

for i, item in enumerate(items):
    print(i, item)
```
使用start以指定起始坐标：
```py
names = ["Alice", "Bob", "Cindy"]

for index, name in enumerate(names, start=1):
    print(index, name)
```
输出：
```py
1 Alice
2 Bob
3 Cindy
```

案例1：和列表推导结合
见[推导式.md](推导式.md)

```py
names = ["Alice", "Bob", "Cindy"]

result = [f"{i}: {name}" for i, name in enumerate(names, start=1)]
print(result)
```
示例 2：找出满足条件的元素位置

```py
nums = [3, 8, 1, 9, 2]

for i, num in enumerate(nums):
    if num > 5:
        print(i, num)
```

==enumerate() 返回的是一个 枚举对象，它是惰性生成的，不会一次性把结果全算出来。==












