数据是所有编程语言的核心
程序的本质是将一些数据（输入）转换成另一些数据（输出）

# Python 常用的数据类型

数据通常被划分为不同的类型

不同类型的数据需要不同的处理方式。
例如，数值数据可以进行加减乘除运算；字符串则更多用于截取、拼接等操作。

常用的数据类型包括：
1. 数值类型
2. 布尔值类型
3. 序列类型
4. 集合类型
5. 映射类型
6. 二进制类型
7. 空值类型

# 判断类型
判断类型主要有两种方式：
1. `type()`：查看对象的精确类型
2. `isinstance()`:判断对象是否属于某个类型或其子类

## `type()`：查看精确类型
适合做“这个对象到底是什么类型”的精确检查。
```py
x = 10
print(type(x))  # <class 'int'>
```

==注意==:type() 只判断完全相同的类型，不考虑继承关系：











