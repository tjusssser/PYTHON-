# 概述

数值型（Numeric Type）用于表示整数、小数、复数等可以参与数学运算的数据。
常见数值类型主要有：
- int :整数
- float:浮点数
- complex：复数
- bool：布尔值
- decimal.Decimal:高精度十进制数
- fractions.Fraction:分数

# 整数 int
整数没有小数部分
```PY
age = 18
temperature = -5
population = 1_400_000
```

为了提高可读性，可以使用==下划线==分隔数字：
```PY
price = 1_299_900
```
下划线不会影响数值：
```PY
print(1_000 == 1000)  # True
```

# 浮点数 `float`
浮点数用于表示带小数的数：
```py
height = 1.75
temperature = -3.5
rate = 0.08
```
也可以使用科学计数法：
```py
light_speed = 3e8       # 3 × 10^8
small_number = 1.5e-4   # 1.5 × 10^-4
```
==浮点数需要注意精度问题==：
```py
print(0.1 + 0.2)
# 0.30000000000000004
```
原因是许多十进制小数无法被二进制浮点数精确表示。

🔺不要直接使用 `==` 判断浮点数是否相等：
```PY
a = 0.1 + 0.2
b = 0.3

print(a == b)  # False
```
可以使用 `math.isclose()`：
```PY
import math

print(math.isclose(a, b))  # True
```

如果涉及金额等必须精确表示的场景，应使用 `Decimal`：
```PY
from decimal import Decimal

price = Decimal("0.10")
total = price + Decimal("0.20")

print(total)  # 0.30
```
🔺注意：创建` Decimal `时优先传入字符串，不要直接传入浮点数。
```PY
from decimal import Decimal

Decimal("0.1")  # 推荐
Decimal(0.1)    # 可能保留浮点数误差
```

# 复数 complex
复数由实部和虚部组成，形式为：
$$a + bj$$
Python 使用 j 表示虚数单位：
```PY
z = 3 + 4j

print(z)
print(z.real)  # 实部：3.0
print(z.imag)  # 虚部：4.0
```
复数支持基本运算：
```py
z1 = 1 + 2j
z2 = 3 + 4j

print(z1 + z2)
print(z1 * z2)
```
共轭复数表示实部相同，虚部相反：
$$\bar{z} = a - bi$$
使用`object.conjugate()`生成共轭复数：
```PY
z = 3 + 4j ;
print( z.conjugate() ) ; # (3-4j)
```
其中：`(3-4j)`只是输出的格式，括号是字符串展示形式。
# 布尔值 bool
布尔值只有两个：
```PY
True
False
```
在 Python 中，bool 是 int 的子类：
```PY
print(True == 1)   # True
print(False == 0)  # True

print(int(True))   # 1
print(int(False))  # 0
```
虽然布尔值可以参与运算，但编程时应优先将它当作逻辑值使用：
```PY
is_adult = age >= 18

if is_adult:
    print("可以进入")
```

# 数值类型的基本运算
```PY
a = 10
b = 3

print(a + b)   # 加法：13
print(a - b)   # 减法：7
print(a * b)   # 乘法：30
print(a / b)   # 真除法：3.333...
print(a // b)  # 整除：3
print(a % b)   # 取余：1
print(a ** b)  # 幂运算：1000
```

注意：/ 的结果通常是 float，而 // 表示向下取整。
负数时要特别注意：
```py
print(-7 // 2)  # -4
```
因为 // 是向负无穷取整，而不是简单截断小数。

# 分数类型 Fraction

如果需要精确表示分数，可以使用 `Fraction`：
Fraction 是 Python 标准库 fractions 里的分数类型，用来精确表示有理数。
```PY
from fractions import Fraction

a = Fraction(1, 3)
b = Fraction(1, 6)

print(a + b)  # 1/2
```

> 为什么需要 Fraction ?
普通浮点数会有精度误差：
```py
print(0.1 + 0.2)
# 0.30000000000000004
```
而 Fraction 能保持精确结果

常见创建方式：
1. 分子和分母
2. 整数 Fraction(5)
3. 字符串 Fraction('2/5')
4. 从浮点数创建 
```PY



























