# 序列类型
序列（Sequence）是一种**有顺序、可按整数索引访问、通常支持切片操作**的数据结构。
| 类型 | 示例 | 可变性 | 主要用途 |
|---|---|---:|---|
| `str` | `"Python"` | 不可变 | 文本 |
| `list` | `[1, 2, 3]` | 可变 | 通用数据集合 |
| `tuple` | `(1, 2, 3)` | 不可变 | 固定结构数据 |
| `range` | `range(5)` | 不可变 | 整数范围、循环 |
| `bytes` | `b"abc"` | 不可变 | 二进制数据 |
| `bytearray` | `bytearray(b"abc")` | 可变 | 可修改的二进制数据 |

# 索引

序列中的元素按照位置排列，索引从 0 开始。
```PY
languages = ["Python", "Java", "C++"]

print(languages[0])   # Python
print(languages[1])   # Java
print(languages[-1])  # C++
```
也可以使用负索引
```TEXT
正索引：  0       1       2
         Python  Java    C++

负索引： -3      -2      -1
```

访问不存在的索引会产生 `IndexError`：
```PY
numbers = [10, 20]

print(numbers[2])  # IndexError
```

不确定序列是否为空时，写：
```PY
if numbers:
  print(number[0]);
```
# 切片
切片用于获取序列的一部分：
```PY
numbers = [0, 1, 2, 3, 4, 5]

print(numbers[1:4])   # [1, 2, 3]
print(numbers[:3])    # [0, 1, 2]
print(numbers[3:])    # [3, 4, 5]
print(numbers[:])     # [0, 1, 2, 3, 4, 5]
```
切片基本格式：
```PY
sequence[start:stop:step]
```
其中：
- start：起始位置，包含
- stop：结束位置，不包含
- step：步长，默认为 1
`STEP`可以为负数，表示从后往前索引：
```PY
numbers = [0, 1, 2, 3, 4, 5, 6]

print(numbers[::2])    # [0, 2, 4, 6]
print(numbers[1::2])   # [1, 3, 5]
print(numbers[::-1])   # [6, 5, 4, 3, 2, 1, 0]
```
切片不会修改原序列。
但需要注意，切片是浅拷贝：
```PY
matrix = [[1, 2], [3, 4]]
copied_matrix = matrix[:]

copied_matrix[0][0] = 100

print(matrix)  # [[100, 2], [3, 4]]
```
如果需要深层复制：
使用`copy.deepcopy()`
```py
from copy import deepcopy

matrix = [[1, 2], [3, 4]]
copied_matrix = deepcopy(matrix)
```
# 序列的通用操作
## 获取长度
```PY
text = "Python"
numbers = [10, 20, 30]

print(len(text))     # 6
print(len(numbers))  # 3
```
## 成员判断






















































